
#include <stdio.h>

#ifndef DRAW_H
#define DRAW_H

// draw level
void drawLevel(Level level, Player player);
char characterChange(Level level, int pos_x, int pos_y);

// draw menus
void drawMenu();



#endif